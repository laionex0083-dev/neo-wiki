# Neo-Wiki 🌳

나무위키 스타일의 위키 엔진 (NamuMark 지원)

## 주요 기능

- 📝 **NamuMark 파서**: `[[링크]]`, `{{{문법}}}`, 각주 등 나무위키 전용 문법 완벽 처리
- 🔗 **위키 라우팅**: `/w/문서명` 형태의 URL 체계
- 🎨 **스킨 시스템**: 다양한 테마 지원
- 📁 **이미지 업로드**: 드래그 앤 드롭 이미지 첨부

## 기술 스택

### Backend
- **Node.js + Express.js**: REST API 서버
- **SQLite (sql.js)**: 경량 데이터베이스 (네이티브 빌드 불필요!)
- **Multer**: 파일 업로드 처리

### Frontend  
- **React + Vite**: 빠른 SPA
- **React Router**: 클라이언트 라우팅
- **Vanilla CSS**: 커스텀 스킨 시스템

### 지원 플랫폼
- ✅ Windows
- ✅ macOS  
- ✅ Linux
- ✅ **라즈베리 파이** (ARM32/ARM64)

## 프로젝트 구조

```
neo-wiki/
├── backend/                    # Express.js API 서버
│   ├── src/
│   │   ├── routes/            # API 라우트
│   │   │   ├── pages.js       # 문서 CRUD
│   │   │   ├── users.js       # 사용자 관리
│   │   │   └── upload.js      # 이미지 업로드
│   │   ├── parser/            # NamuMark 파서
│   │   │   └── namumark.js    # 핵심 파서 엔진
│   │   ├── database/          # 데이터베이스
│   │   │   ├── init.js        # DB 초기화
│   │   │   └── models.js      # 데이터 모델
│   │   ├── middleware/        # 미들웨어
│   │   └── app.js             # Express 앱
│   ├── uploads/               # 업로드된 파일
│   └── package.json
│
├── frontend/                   # React SPA
│   ├── src/
│   │   ├── components/        # UI 컴포넌트
│   │   │   ├── Header.jsx
│   │   │   ├── WikiRenderer.jsx
│   │   │   └── Editor.jsx
│   │   ├── pages/             # 페이지 컴포넌트
│   │   │   ├── PageView.jsx   # 문서 보기
│   │   │   ├── PageEdit.jsx   # 문서 편집
│   │   │   └── PageHistory.jsx
│   │   ├── skins/             # 스킨 시스템
│   │   │   ├── default/
│   │   │   └── dark/
│   │   ├── hooks/
│   │   └── App.jsx
│   └── package.json
│
├── docker-compose.yml
└── README.md
```

## 빠른 시작

```bash
# 1. 의존성 설치
cd backend && npm install
cd ../frontend && npm install

# 2. 백엔드 서버 시작 (포트 3001)
cd backend && npm run dev

# 3. 프론트엔드 시작 (포트 5173)
cd frontend && npm run dev
```

## NamuMark 지원 문법

| 문법 | 설명 | 예시 |
|------|------|------|
| `[[문서명]]` | 내부 링크 | `[[대문]]` |
| `[[문서명\|표시]]` | 링크 표시문구 | `[[나무위키\|위키]]` |
| `'''굵게'''` | 굵은 글씨 | `'''강조'''` |
| `''기울임''` | 기울인 글씨 | `''이탤릭''` |
| `~~취소~~` | 취소선 | `~~삭제~~` |
| `__밑줄__` | 밑줄 | `__강조__` |
| `{{{코드}}}` | 코드/리터럴 | `{{{inline}}}` |
| `[* 각주]` | 각주 | `[* 주석]` |
| `[include(틀명)]` | 틀 삽입 | `[include(기본틀)]` |
| `= 제목 =` | 제목 (1~6단계) | `== 소제목 ==` |
| ` * 목록` | 글머리 기호 | ` * 항목` |
| ` 1. 번호` | 번호 목록 | ` 1. 첫째` |
| `> 인용` | 인용문 | `> 인용글` |
| `----` | 수평선 | `----` |

## 🍓 라즈베리 파이 배포

```bash
# 자동 설치 (권장)
chmod +x install.sh
./install.sh

# 또는 수동 설치
cd backend && npm install --production
cd ../frontend && npm install && npm run build

# 프로덕션 실행
npm run start:prod
```


자세한 내용은 [라즈베리 파이 배포 가이드](docs/RASPBERRY_PI.md)를 참조하세요.

## 🐳 Docker 배포

```bash
docker-compose up -d
```

## 라이선스

MIT License
